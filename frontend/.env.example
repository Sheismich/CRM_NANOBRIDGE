# En desarrollo NO hace falta: el proxy de Vite (vite.config.ts) sirve
# /api en el mismo origen apuntando a http://localhost:3000 por default.
# Solo se usa si el backend local corre en otro puerto, o para apuntar el
# build de producción al backend real (Cloud Run):
# VITE_API_URL=https://nanobridge-api-165032456965.us-central1.run.app
